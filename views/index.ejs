<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full text-white p-10 min-h-screen bg-zinc-900">
        <div class="form">
            <form action="/create" method="post">
                <input class="block px-5 w-full py-2 bg-zinc-800 rounded-md outline-none" type="text" placeholder="title" name="title">
                <textarea class="block resize-none  w-full rounded-md outline-none mt-2 px-5 py-2 bg-zinc-800" name="detail" placeholder="write your task "></textarea>
                <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 rounded-md" value="Create Task">
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap py-10">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task min-w-72  px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%=val%></h1>
                        <div class="flex justify-between center mt-4">
                            <a class="text-blue-600" href="/files/<%= val %>">read more</a>
                            <a class="text-zinc-600 " href="edit/<%= val %>">edit</a>
                        </div>
                    </div>
                    <% }) %>
            <% } else{%>
                <h3 class="text-zinc-500">no task yet</h3>
            <%} %>
            
           
            
        </div>
    </div>
</body>
</html>